# Meshaway environment variables
# Copy to .env and adjust as needed

# Agent/agent specifier (e.g. gemini-cli). Legacy: MESH_AGENT also supported.
# MESH_AGENT=gemini-cli

# Bridge listen address (host:port)
# MESH_LISTEN=127.0.0.1:4321

# Hub URL (when bridge connects to hub)
# MESH_HUB=http://127.0.0.1:7337

# Hub listen address (when starting hub)
# MESH_HUB_LISTEN=127.0.0.1:7337
